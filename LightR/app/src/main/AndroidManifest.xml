<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="group.cs2001.lightr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainMenu"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainTemp"
            android:label="@string/title_activity_main_temp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainLight"
            android:label="@string/title_activity_main_light"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainDevices"
            android:label="@string/title_activity_main_devices"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>