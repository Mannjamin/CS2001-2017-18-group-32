<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="group.cs2001.lightr">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainStartupSettings"
            android:label="Initial Setup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainMenu"
            android:label="LightR"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainTemp"
            android:label="Temperature"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainLight"
            android:label="Lighting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainDevices"
            android:label="Devices"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainSound"
            android:label="Sound"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainSettings"
            android:label="Global Settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainLightSettings"
            android:label="Light Settings"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>